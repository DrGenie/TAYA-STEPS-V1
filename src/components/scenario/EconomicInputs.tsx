import { useScenarioStore } from '../../state/scenarioStore';
import { Field } from '../common/Field';
export function EconomicInputs(){const s=useScenarioStore(x=>x.scenario),update=useScenarioStore(x=>x.updateScenario);return <div className="grid cols-2"><Field label="Incremental probability of meaningful improvement (%)" evidenceId="improvement-prob"><input type="number" min="0" max="100" value={s.improvementProbability*100} onChange={e=>update({improvementProbability:Number(e.target.value)/100})}/></Field><Field label="QALY gain per additional improved young person" evidenceId="qaly-gain"><input type="number" min="0" max="1" step="0.005" value={s.qalyGain} onChange={e=>update({qalyGain:Number(e.target.value)})}/></Field><Field label="Illustrative value per QALY (AUD)" evidenceId="qaly-value"><input type="number" min="0" step="1000" value={s.qalyValue} onChange={e=>update({qalyValue:Number(e.target.value)})}/></Field><Field label="Avoided downstream health-service expenditure (AUD)" evidenceId="avoided-cost"><input type="number" min="0" step="10" value={s.avoidedCost} onChange={e=>update({avoidedCost:Number(e.target.value)})}/></Field><Field label="Broader educational/productivity benefit per completer (AUD)" hint="Off by default. Entering a positive value activates this explicit assumption."><input type="number" min="0" step="10" value={s.broaderBenefitPerCompleter} onChange={e=>update({broaderBenefitPerCompleter:Number(e.target.value)})}/></Field><Field label="Discount rate for multi-year economic scenarios (%)"><input type="number" min="0" max="5" step="0.1" value={s.discountRate*100} onChange={e=>update({discountRate:Number(e.target.value)/100})}/></Field></div>;}
