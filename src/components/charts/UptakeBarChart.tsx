import { AccessibleChart } from './AccessibleChart';
import { formatPercent } from '../../utils/format';
export function UptakeBarChart({youth,parent,family}: {youth:number;parent:number;family:number}){const rows=[{group:'Youth',value:youth},{group:'Parent/carer',value:parent},{group:'Family-compatible',value:family}];return <AccessibleChart title="Modelled uptake" description="Illustrative prototype probability of selecting the support pathway rather than no additional support." rows={rows} columns={[{key:'g',header:'Perspective',render:r=>r.group},{key:'v',header:'Uptake',render:r=>formatPercent(r.value)}]} option={{xAxis:{type:'value',max:1,axisLabel:{formatter:(v:number)=>`${v*100}%`}},yAxis:{type:'category',data:rows.map(r=>r.group)},series:[{type:'bar',data:rows.map(r=>r.value),label:{show:true,formatter:(p:any)=>`${(Number(p.value)*100).toFixed(1)}%`},itemStyle:{color:'#2F6B9A'}}],tooltip:{trigger:'axis'}}}/>;}
