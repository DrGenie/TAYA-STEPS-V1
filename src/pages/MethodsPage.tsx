import { PageHeader } from '../components/common/PageHeader';
import { useScenarioStore } from '../state/scenarioStore';
import { runIntegrityChecks } from '../engine/validation';
import { DataTable } from '../components/common/DataTable';
export function MethodsPage(){const s=useScenarioStore(x=>x.scenario);const checks=runIntegrityChecks(s);return <div className="page container"><PageHeader title="Methods" lead="Transparent methods for the TAYA-STEPS research prototype."/><section id="choice-model"><h2>Choice model</h2><p>The prototype uses separate youth and parent/carer systematic utility functions. Numerical coefficients are calibrated synthetic values informed by published preference literature. They are not copied or transferred estimates from another DCE because attributes, populations, currencies and utility scales differ.</p><div className="equation">Vg(x) = ASCg + setting + βwait,g × waitWeeks + delivery + parent involvement + professional support + follow-up + βcost,g × costIn10DollarUnits + subgroup interactions</div><div className="equation">P(use support) = exp(V) / [1 + exp(V)]</div><p>For two support alternatives A and B plus neither, a numerically stable softmax is used. Wittevrongel and colleagues support the importance of cost, waiting time and service format, while Henderson and colleagues demonstrate heterogeneity in youth service preferences. The TAYA-STEPS numerical coefficients remain synthetic.</p></section><h2>Family decision rules</h2><p>Users may view youth or parent perspectives, a balanced utility rule, a youth-centred utility rule, or a parent-constrained minimum-probability rule. Combined rules are prototype decision rules, not empirical dyadic models.</p><h2>Heterogeneity and equity</h2><p>Only specified prototype modifiers are applied for elevated anxiety symptoms, previous service use, regional/rural residence and household economic resources. No synthetic preference differential is imposed by gender, sexual orientation, cultural background or First Nations status because a defensible TAYA-specific parameter is not available.</p><h2>Population and reach</h2><div className="equation">offered = target population × offer rate; starters = offered × modelled uptake; completers = starters × completion rate</div><p>Referral stages are applied to starters and bounded so no downstream stage can exceed its upstream population.</p><section id="capacity"><h2>Costing and capacity</h2><p>Service-provider resource cost is calculated from pathway-specific staff minutes and loaded hourly resource costs. Australian Government MBS fees are shown separately as payer reference values and are not treated as opportunity costs.</p><div className="equation">productive hours per FTE = paid hours/week × working weeks/year × productive direct-service share</div><div className="equation">FTE required = required direct-service hours / productive hours per FTE</div></section><h2>Budget impact</h2><p>Annual variable resource costs, fixed setup costs, annual maintenance and nominal cost growth are reported over 1, 2, 3 or 5 years. Ordinary budget-impact totals remain undiscounted unless a separate present-value analysis is requested.</p><h2>Exploratory economic scenario analysis</h2><div className="equation">additional improved = completers × incremental improvement probability</div><div className="equation">health value = additional improved × QALY gain × value per QALY</div><div className="equation">net benefit = health value + health-service offset + optional broader benefit - economic cost</div><div className="equation">BCR = total monetised benefit / economic cost</div><p>These are exploratory calculations using external assumptions. They are not estimates of TAYA effectiveness or cost-effectiveness. The illustrative value per QALY is not presented as an official Australian threshold.</p><h2>Uncertainty and sensitivity</h2><p>Monte Carlo analysis uses a deterministic seed. Preference uncertainty is represented around prototype coefficients, resource costs vary over stated ranges, completion and referral assumptions vary around their central values, and economic assumptions vary within stated bounds. One-way sensitivity and threshold analyses are provided with tables.</p><h2>Optimisation</h2><p>The Optimiser enumerates logically feasible combinations of the seven service attributes, applies user constraints, calculates uptake, reach, cost, FTE and equity, and identifies non-dominated options. No policy-value weights are silently assigned.</p><h2>Future parameter replacement</h2><p>Future TAYA coefficient sets can be imported as validated JSON, stored locally and restored to the prototype set at any time. Saved scenarios record application version, parameter-set version, evidence review date, simulation seed and draw count.</p><h2>Model integrity checks</h2><DataTable caption="Current model integrity checks" rows={checks} columns={[{key:'c',header:'Check',render:r=>r.name},{key:'s',header:'Status',render:r=>r.passed?'Passed':'Failed'},{key:'d',header:'Detail',render:r=>r.detail}]}/><p className="small">These are internal model integrity checks, not external validation.</p></div>;}
